# Pi Agent 评测协议

评测固定记录 case、画布快照、用户目标、模型版本、Skill 版本、工具序列、确认次数、点数、延迟、人工修订和质量评分。A/B 每次只改变一个版本变量；越权、未经确认的高风险写入、重复副作用和终态不可恢复均为硬失败。

离线结果输出结构化 trace diff，并对质量提升、点数增量、完成延迟和人工修订量做门禁。线上灰度使用同一字段，禁止用模型自报费用覆盖 Billing 的实际账本。
